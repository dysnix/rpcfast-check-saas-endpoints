[package]
name = "beam-quic-check"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
beam-quic-client = "0.1.1"
rustls = "0.23.43"
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread"] }
